{
  "name": "w-typing-fight",
  "version": "1.0.0",
  "private": true,
  "description": "Type Fighter - 2人対戦型タイピングゲーム",
  "scripts": {
    "dev": "pnpm -r --parallel dev",
    "build": "pnpm -r build",
    "web:dev": "pnpm --filter web dev",
    "workers:dev": "pnpm --filter workers dev",
    "lint": "pnpm -r lint",
    "type-check": "pnpm -r type-check"
  },
  "keywords": [
    "typing-game",
    "websocket",
    "cloudflare-workers",
    "react"
  ],
  "author": "",
  "license": "MIT",
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  }
}
